function initialState() {
  return {
  };
}

const getters = {
};

const mutations = {
};

const actions = {
};

export default {
  namespaced: true,
  state: initialState,
  getters,
  mutations,
  actions
};
